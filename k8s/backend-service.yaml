apiVersion: v1
kind: Service
metadata:
  name: mini-backend-service
spec:
  type: NodePort
  selector:
    app: mini-backend
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
      nodePort: 30080
